apiVersion: consul.hashicorp.com/v1alpha1
kind: PeeringDialer
metadata:
  name: dc1
  namespace: default
spec:
  peer:
    secret:
      name: "peering-token"
      key: "data"
      backend: "kubernetes"
